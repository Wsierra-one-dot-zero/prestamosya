"""
Django settings for prestamosya project.

Generated by 'django-admin startproject' using Django 5.2.2.

For more information on this file, see
https://docs.djangoproject.com/en/5.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/5.2/ref/settings/
"""

from pathlib import Path
import decimal
import os
from dotenv import load_dotenv
load_dotenv()
from .secrets_manager import get_secret

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/5.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'django-insecure-w!%ktlcl24+b)e3v*0*k4+$ykq00i=a&5t)5%97vud8c2u!%6n'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

# Configuración dinámica de ALLOWED_HOSTS para ECS
import socket
ALLOWED_HOSTS = [
    '.elb.amazonaws.com',  # Para el Application Load Balancer
    'localhost',
    '127.0.0.1',
    socket.gethostname(),  # Nombre de host de la tarea ECS
]


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'prestamosapp'
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'prestamosya.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'prestamosya.wsgi.application'


# Database
# https://docs.djangoproject.com/en/5.2/ref/settings/#databases

# Obtener credenciales de la base de datos desde AWS Secrets Manager
db_secret = get_secret(os.getenv('DB_SECRET_NAME'))

# Configuración de la base de datos
if db_secret and 'username' in db_secret and 'password' in db_secret:
    DATABASES = {
        'default': {
            'ENGINE': 'django.db.backends.mysql',
            'NAME': db_secret.get('dbname', 'loans'),
            'USER': db_secret['username'],
            'PASSWORD': db_secret['password'],
            'HOST': db_secret.get('host', 'my-mysql-db.cup8qceyub9n.us-east-1.rds.amazonaws.com'),
            'PORT': db_secret.get('port', '3306'),
        }
    }
else:
    raise Exception("No se pudieron obtener las credenciales de la base de datos desde AWS Secrets Manager. Verifique que la variable DB_SECRET_NAME está configurada correctamente.")


# Password validation
# https://docs.djangoproject.com/en/5.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/5.2/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/5.2/howto/static-files/

STATIC_URL = 'static/'

# Ruta donde Django colocará los archivos recolectados por collectstatic
STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')

# Default primary key field type
# https://docs.djangoproject.com/en/5.2/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# Authentication settings
LOGIN_URL = 'login'  # URL de la vista de login
LOGIN_REDIRECT_URL = 'home'  # Página a la que redirigir después del login
LOGOUT_REDIRECT_URL = 'home'  # Página a la que redirigir después del logout


decimal.getcontext().prec = 10